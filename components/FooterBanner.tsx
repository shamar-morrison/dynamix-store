import React from 'react';

interface Props {
  footerBanner: any;
}

const FooterBanner = ({ footerBanner }: Props) => {
  return (
    <div>
      <p>Footer banner</p>
    </div>
  );
};

export default FooterBanner;
